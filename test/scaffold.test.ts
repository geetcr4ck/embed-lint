import { describe, expect, it } from 'vitest';

// Phase 0 dummy test - keeps the vitest toolchain green.
// Real validator/serializer tests arrived in Phase 1-2.
describe('scaffold Phase 0', () => {
  it('placeholder is active', () => {
    expect('EmbedLint scaffold Phase 0').toContain('Phase 0');
  });
});
